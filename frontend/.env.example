# Frontend API Configuration
VITE_API_URL=/api
VITE_DEV_PROXY_TARGET=http://localhost:8000

# Use /api in local and production so one build works behind Nginx.
# VITE_DEV_PROXY_TARGET is only for `npm run dev`.
